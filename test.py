import time

start_time = time.time()
# blahblah
end_time = time.time()
execution_time = end_time - start_time
print("Execution time:", execution_time)
# comp

coefficients = [0, 1.0, 10.0]
regularization_type = ['entropy']
results = []
num_epoch = 1
batch_size = 1024

compressibleNN_list[0].net_model.layers[2].variables[0]
import tensorflow as tf
import numpy as np
import dahuffman
import pickle

from tensorflow import keras

# new task 
# one main jupyter notebook entroy -> saves model -> load model

# two jupyter notebook
# one of them saves model with two files weights, codec
# one of them loads model
# save an object itself ( everything )

# x axis model size
# y accuracy
# fig 7
# ramda if too high too compress

# CNN
# benchmark
